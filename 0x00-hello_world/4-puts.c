#include <stdio.h>

/**
*main - use puts instead of printf
*the puts functionn prints an output followed by  a new line
*Return: printf prints a long output
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
